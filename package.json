{
  "name": "calendar-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker bridge between OpenClaw and Google Calendar.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "prettier": "^3.2.5",
    "wrangler": "^3.60.1"
  }
}
